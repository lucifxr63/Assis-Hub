# Copia este archivo como .env.local y ajusta los valores.

# URL base del middleware backend
NEXT_PUBLIC_API_URL=http://localhost:18791

# Modo mock: true = sin backend, false = backend real
NEXT_PUBLIC_USE_MOCK=true
